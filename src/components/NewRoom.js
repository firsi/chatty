import React from 'react';

export const NewRoom = () => {

    return(
        <div className="createRoom" > 
            <span>+</span>
            <span>Add a new channel</span>
        </div>
    )
}